const chai = require("chai");
const expect = chai.expect;
const App = require("../src/components/App/App.js")

const sampleData = require('../sample-data');

describe('App', function() {
  let app, list, movie;

  beforeEach(function() {
    app = new App()
  })

})